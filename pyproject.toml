[project]
name = "arxiv2kindle"
version = "1.3.0"
requires-python = ">=3.13"
dependencies = [
    "lxml",
    "requests",
    "typer",
    "wget",
]

[project.scripts]
arxiv2kindle = "arxiv2kindle:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
